$(function() {
  if(window.location.href.match(/\/battle_members/)) {

    // $.get(`/api/v1/topics/${tagSlug}`, {
    //       page: nextPageNumber,
    //       sort_by: sortBy
    //     }).then(function(response) {
  }
});
